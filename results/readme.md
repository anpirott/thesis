This folder contains all the saved results of the models we trained during the experiments, fine-tuning and final evaluation.

The hierarchy is the same for both models : \[default_models|fine_tuned_models|final_models\]/model_name/\[MIST|PARSEC\]/filtering/.
Inside thes folders, the metrics are in a sub-folder and the plots are in the folder itself.