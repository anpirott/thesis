The [final_models](final_models) folder contains the models which were trained with the whole dataset.

The [fine_tuned_models](fine_tuned_models) folder contains the models which were only fine-tuned and not trained with the whole dataset.