fully_trained contains the models which are trained with the whole dataset (for the moment, only MIST).

Kfold_trained contains the models from the Kfold training to be able to reuse them without training everything again. (pas sûr que ce soit faisable, plutôt sauvegarder les résultats de kfold)
