This folder contains all the saved predictions of the models we trained during the experiments, fine-tuning and final evaluation.

The hierarchy is the same for both models : \[default_models|fine_tuned_models|final_models\]/model_name/\[MIST|PARSEC\]/filtering/.
Inside thes folders, the ".npy" filenames contain what the file represents.